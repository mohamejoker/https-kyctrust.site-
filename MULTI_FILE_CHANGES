// app/page.tsx
import { SEOHead } from "@/components/seo/seo-head";
import { PerfHints } from "@/components/perf/perf-hints";
import SiteHeader from "@/components/layout/site-header";
import BlocksRenderer from "@/components/blocks/blocks-renderer";
import SiteFooter from "@/components/layout/site-footer";
import ChatbotWidget from "@/components/chatbot-widget";

export default function Page() {
  const locale = "en"; // Assume locale is defined somewhere

  return (
    <div className="min-h-screen bg-gradient-to-b from-neutral-50 via-emerald-50/40 to-white text-neutral-900 dark:from-neutral-950 dark:via-neutral-950 dark:to-neutral-950 dark:text-neutral-100">
      <SEOHead />
      <PerfHints />
      <SiteHeader locale={locale} />
      <main id="main-content">
        <BlocksRenderer />
      </main>
      <SiteFooter locale={locale} />
      <ChatbotWidget />
    </div>
  );
}

// components/layout/site-header.tsx
import React from "react";
import FeatureIcon from "@/components/icons/feature-icon";
import GradientButton from "@/components/ui/gradient-button";
import Button from "@/components/ui/button";
import AnimatedIcon from "@/components/ui/animated-icon";

export default function SiteHeader({ locale }) {
  return (
    <header className="bg-background">
      <a
        href="#main-content"
        className="sr-only focus:not-sr-only focus:fixed focus:top-3 focus:left-3 focus:z-[60] focus:rounded-lg focus:bg-background focus:px-3 focus:py-2 focus:shadow"
      >
        {locale === "ar" ? "تخطي إلى المحتوى" : "Skip to content"}
      </a>
      <nav className="hidden items-center gap-6 md:flex">
        <a href="#services" className="text-sm text-muted-foreground hover:text-foreground focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-emerald-500/60 rounded">
          {locale === "ar" ? "الخدمات" : "Services"}
        </a>
        <a href="#faq" className="text-sm text-muted-foreground hover:text-foreground focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-emerald-500/60 rounded">
          {locale === "ar" ? "الأسئلة" : "FAQ"}
        </a>
        <a href="#contact" className="text-sm text-muted-foreground hover:text-foreground focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-emerald-500/60 rounded">
          {locale === "ar" ? "تواصل" : "Contact"}
        </a>
      </nav>
      {/* rest of code here */}
    </header>
  );
}

// components/blocks/cta-block.tsx
import React from "react";
import Button from "@/components/ui/button";
import GradientButton from "@/components/ui/gradient-button";

export default function CTABlock({ data }) {
  return (
    <div className="flex flex-col items-center justify-center space-y-4">
      <GradientButton className="min-w-[200px]" size="lg">
        {data.cta.primaryText}
      </GradientButton>
      <Button variant="outline" size="lg" className="min-w-[200px] focus-visible:ring-2 focus-visible:ring-emerald-500/60 bg-transparent">
        {data.cta.secondaryText}
      </Button>
      {/* rest of code here */}
    </div>
  );
}

// components/blocks/features-block.tsx
import React from "react";
import FeatureIcon from "@/components/icons/feature-icon";
import AnimatedIcon from "@/components/ui/animated-icon";

export default function FeaturesBlock({ features }) {
  return (
    <div className="grid grid-cols-1 gap-8 md:grid-cols-3">
      {features.map((f) => (
        <div key={f.id} className="flex flex-col items-center text-center">
          <AnimatedIcon className={`mb-5 inline-flex h-12 w-12 items-center justify-center rounded-xl bg-gradient-to-br ${f.palette.range} text-white shadow`}>
            <FeatureIcon name={f.icon} className="h-6 w-6" />
          </AnimatedIcon>
          <h3 className="text-lg font-semibold">{f.title}</h3>
          <p className="text-sm text-muted-foreground">{f.description}</p>
        </div>
      ))}
      {/* rest of code here */}
    </div>
  );
}

// components/layout/site-footer.tsx
import React from "react";

export default function SiteFooter({ locale }) {
  return (
    <footer className="mt-10" aria-label={locale === "ar" ? "تذييل الموقع" : "Site footer"}>
      {/* rest of code here */}
    </footer>
  );
}

// components/blocks/hero-block.tsx
import React from "react";
import Button from "@/components/ui/button";

export default function HeroBlock({ data }) {
  return (
    <section role="banner" className="bg-cover bg-center py-16">
      <div className="container mx-auto flex flex-col items-center justify-center text-center">
        <h1 className="text-4xl font-bold">{data.hero.title}</h1>
        <p className="text-lg text-muted-foreground">{data.hero.description}</p>
        <Button variant="outline" size="lg" className="min-w-[200px] focus-visible:ring-2 focus-visible:ring-emerald-500/60 bg-transparent">
          {data.hero.cta.secondaryText}
        </Button>
        <Button size="lg" className="min-w-[200px] focus-visible:ring-2 focus-visible:ring-emerald-500/60">
          {data.hero.cta.primaryText}
        </Button>
      </div>
    </section>
  );
}
